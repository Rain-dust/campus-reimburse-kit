# 安全说明

## 报告漏洞

请不要在公开 Issue 中附带真实发票、个人信息、API 密钥或可复现的私人工作区。

发现路径穿越、任意文件读取、恶意压缩包、公式注入或本地代码执行问题时，请通过 GitHub Security Advisory 私下报告，并提供不含隐私数据的最小复现。

## 安全边界

- 本工具是本地单用户桌面应用，不应直接暴露到公网。
- OCR 输出不可信，所有金额和明细在报销前必须人工核对。
- Excel 模板和导出文件可能包含宏、公式或外部内容；只使用可信模板。
- 便携版应从可信发布来源下载并校验哈希。

目前不承诺固定的安全支持周期；最新主分支是优先修复目标。
