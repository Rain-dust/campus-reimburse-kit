# 贡献指南

感谢你改进 Campus Reimburse Kit。

## 开始之前

1. 不要提交真实发票、付款记录、数据库、工作区或云服务密钥。
2. 测试样例必须脱敏，供应商、发票号和人员信息应使用虚构内容。
3. 修改模板前请说明模板来源、授权情况和适用单位。
4. 保持本地优先设计，OCR 失败不能阻塞人工整理流程。

## 开发流程

```bat
scripts\setup_windows.bat
scripts\check_windows.bat
```

提交前至少确保：

- 编译检查通过。
- 全部单元测试通过。
- `git diff --check` 无错误。
- 没有新增票据、数据库、模型文件或构建产物。

## Pull Request

请在 PR 中说明问题、实现方式、验证命令和可能的兼容性影响。涉及 Excel 导出时，请同时说明是否保持表头、公式、打印布局和签章区域。
